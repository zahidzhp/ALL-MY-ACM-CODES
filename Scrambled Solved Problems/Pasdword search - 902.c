#include <stdio.h>
#include <string.h>

int main()
{
    int b;
    char a[10];
    char sub[10][10];
    while(scanf("%d",&b)==1)
    {
        scanf("%s",&a);
        int len;
        len=strlen(a);

    }
}
